#!/usr/bin/env node

const lines = [
  "",
  "=== Lattice RBAC — New Firestore Database Setup ===",
  "",
  "1) Install Node dependencies (from backend_re2/rbac):",
  "     npm install",
  "",
  "2) Copy environment file:",
  "     copy .env.example .env",
  "",
  "3) PRODUCTION — create named database (does NOT touch default):",
  "     npx firebase-tools@latest firestore:databases:create rbac-new-db \\",
  "       --location=asia-southeast1 --project=lattice-2026",
  "",
  "   Verify:",
  "     npx firebase-tools@latest firestore:databases:list --project=lattice-2026",
  "",
  "4) Deploy RBAC rules + indexes to the NEW database only:",
  "     cd ..",
  "     npx firebase-tools@latest deploy --only firestore:rbac-new-db --project=lattice-2026",
  "",
  "5) EMULATOR — start suite from backend_re2 root:",
  "     npx firebase-tools@latest emulators:start --only firestore,auth",
  "",
  "6) Seed SOURCE database (default) if empty — in another shell:",
  "     set USE_FIRESTORE_EMULATOR=true",
  "     python seed_db.py",
  "",
  "7) Clone READ (default) -> WRITE (rbac-new-db):",
  "     cd rbac",
  "     npm run clone:emulator:dry-run",
  "     npm run clone:emulator",
  "",
  "SAFETY: cloneDB.js never writes to SOURCE_DATABASE.",
  "",
];

console.log(lines.join("\n"));
